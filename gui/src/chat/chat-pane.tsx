import type { GuiSession } from "../session/types.js";

/** A008-0034 replaces this stub. Keep the ChatPane export. */
export function ChatPane(_props: { readonly session: GuiSession }) {
  return <section className="a008-chat">Chat</section>;
}
