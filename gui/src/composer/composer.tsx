import type { GuiSession } from "../session/types.js";

/** A008-0035 replaces this stub. Keep the Composer export. */
export function Composer(_props: { readonly session: GuiSession }) {
  return <section className="a008-composer">Composer</section>;
}
