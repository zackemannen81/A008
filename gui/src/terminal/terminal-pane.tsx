/** A008-0036 replaces this stub. Keep TerminalPane and runShellCommand. */
export async function runShellCommand(_command: string): Promise<string> {
  return "";
}

export function TerminalPane() {
  return <section className="a008-terminal">Terminal</section>;
}
