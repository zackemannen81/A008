import type { GuiSession } from "../session/types.js";

/** A008-0037 replaces this stub. Keep the SettingsPane export. */
export function SettingsPane(_props: { readonly session: GuiSession }) {
  return <section className="a008-settings">Settings</section>;
}
