# Task A008-0030 — A008-owned GUI program

Status: In Progress
Owner: Grok (operator / boss)
Created: 2026-09-02

Parent program. `docs/CURRENT_TASK.md` on `main` stays the empty template.

## Goal

A launchable A008 GUI, adapted from OpenHands Canvas UX, that talks only to
A008 through `src/gui-host/` and `A008-acp`. No OpenHands source mutation.
No credentials in the renderer.

## Children

| Task | Module |
| --- | --- |
| A008-0031 | ADR 0019 + stub shell (this landing) |
| A008-0032 | `src/gui-host/` |
| A008-0033 | `gui/src/session/` |
| A008-0034 | `gui/src/chat/` |
| A008-0035 | `gui/src/composer/` |
| A008-0036 | `gui/src/terminal/` |
| A008-0037 | `gui/src/settings/` and `gui/src/brand/` |

## Out of scope

- Live NVIDIA GUI E2E
- Desktop packaging
- Copying OpenHands `src/`
- LangChain
- Agent Server as product runtime

## Definition of done

`npm run gui-host` (or equivalent) serves the GUI, opens an ACP session, streams
thought and answer from a fake transport, runs `/shell` in the host, and never
puts `NVIDIA_API_KEY` on the WebSocket.
