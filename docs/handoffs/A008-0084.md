# A008-0084 — Readable memory relationship map

Status: Complete
Date: 2026-09-08
Operator: Codex
Branch: codex/readable-memory-map
Integration: owner-authorized direct main publication.

## Result

The existing SVG now packs spaced primary-domain clusters around a stable hub.
Selection preserves coordinates, highlights actual links and shows full content
in the inspector. Collision-aware labels avoid dense overprinting. Kind colours
remain visible; dashed rings identify dormant evidence. Focus frames only the
selected neighbourhood; zoom/scroll and Fit remain local presentation controls.

The inspector follows incoming/outgoing/self links by exact stored endpoint and
keeps parallel relations distinct. Counts mean displayed stored links. Its raw
content section starts collapsed. No synthetic relationship, similarity score,
new retrieval route or lifecycle write is introduced. Existing graph bounds,
provider owners, user databases/settings and owner logo CSS are unchanged.

## Evidence

- GUI typecheck/build and 118/118 GUI tests pass without skips.
- Eight inspection core tests pass, including actual local HTTP/ACP inspection
  with no model call or SQLite mutation.
- New geometry/render tests cover 80 nodes in one/six/many domains, spacing,
  order independence, stable selection and non-overlapping chosen labels.
- Browser: 1720x1100, 1366x900 and 390x844; default 80 nodes/152 links, dense
  80/240, one/many domains and empty cases. No page errors or Vite overlay.
- Selection keeps 80 transforms stable; focus of fixture 1 renders 15 nodes and
  15 incident edges only. Keyboard Space, connected-record navigation, zoom,
  Fit, search, domain filter and Clear filters work. No horizontal page overflow.
- The narrow focus frame was visually checked after increasing graph type sizes.
  Small screens still use zoom/focus/filter for dense content.

The [preview recipe](../../gui/test/README.md) and fixture run the real MemoryPage
and parser against synthetic records. No real memory or provider is required;
its test route does not exist in the production build. Screenshots were inspected
locally; they are visual test evidence, not a claim about the owner's current data.

The earlier semantic repair is in [A008-0083](A008-0083.md). Its 524 core tests
passed before this presentation-only task. Live model compliance remains untested.

## Resume

No blocker. The owner can start the updated application and review Memory /
Relationship map using their actual store. This task did not restart the app,
change user settings/databases, or alter the frozen L1-L3 target.
