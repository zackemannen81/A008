# Handoff - A008-0098

Status: Complete, merged into remote main
Task ID: A008-0098
Branch: `A008/a008-0098-auth-recovery`

## Outcome

Expired standalone PIN authentication no longer leaves fresh GUI HTTP actions showing raw `Authentication required.` errors. The bundled GUI centrally recognizes only the exact same-origin host PIN-gate 401 and navigates back to `/`, reusing the existing login page.

Native `#engine=` capability auth, cross-origin responses and unrelated 401 responses are unchanged. The original response body remains readable because inspection uses `Response.clone()`.

## Verification

- Full `npm test`: 564 core, 4 membership, 159 GUI; all passed.
- Existing standalone PIN host regressions: 3/3 passed.
- GUI typecheck and production build passed.
- `git diff --check` passed.
- No live provider call, push or deployment.

## Integration note

This branch is based on the local A008-0097 implementation commit `79b04a8`; integrate in program order. The 24-hour PIN cookie lifetime and process-lifetime auth token are intentionally unchanged.

## Operator integration - 2026-09-14

Merged into remote main through [PR #43](https://github.com/zackemannen81/A008/pull/43),
merge commit 2ee3105a254fc3d96a2baef073ea84213f44d35f. This supersedes the
earlier pending-integration notes above. Combined verification: 564 core,
4 membership and 161 GUI tests passed; production GUI build/typecheck passed.
