# A008-0068 handoff

Status: Complete
Date: 2026-09-07
Branch: `codex/A008-0068-gui-repository-tools`
Identity: claimed on main in `7a84c85` before charter freeze.

## Delivered

Shared native file/Git tools plus standalone GUI catalog, workspace display,
explicit model-action buttons and tool activity. Real permission dialogs and
provider observations use the existing ACP bridge. No external frontend needed.
The active workspace can be set through `A008_GUI_WORKSPACE` at host startup.

## Verification

557 full-suite tests pass. Final targeted native/standalone suite 10/10 and
host suite 32/32 pass. GUI production build and desktop/mobile browser proof pass.
See [evidence](../evidence/A008-0068_gui-repository-tools.md).

## Run and review

Stop the older standalone GUI host, then run `npm run gui` in the A008 checkout.
Connect and open Tools → Repository. See [runbook](../GUI_REPOSITORY_TOOLS.md).
The shipped global instruction setting remains unchanged. A Codex-specific
prompt must use actual offered tool names; this is not a Codex API clone.

No push, PR, merge, provider spend or release performed for this task.
Implementation is left in the working tree for review. CURRENT_TASK is restored
from the template. The operator appends the journal on merge.
