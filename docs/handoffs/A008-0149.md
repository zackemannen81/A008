# A008-0149 Handoff — Stage 5 independent client SDK and bundled web migration

Status: Complete
Task: A008-0149
Parent: A008-0103
Branch: `grok/A008-0149-stage5-client-sdk`
Implementation base: main `24ff390` plus claim `7a3eefb`
Pull request: open after push

## Delivered

- `packages/client` (`@a008/client`) is an independently installable SDK over `@a008/protocol`.
- Injected fetch, WebSocket constructor and cookie/bearer/engine credential adapters. V2 tickets and device secrets are not placed in URLs.
- Bundled GUI session, memory, upload, shell, projects, catalogs, MCP, images, frame-check and runtime-capability I/O consume the SDK. `use-gui-session.ts` remains the React adapter.
- Bundled chat keeps the V1 session adapter so PIN-disabled standalone, engine-panel capability, prompt attachments and in-session image generation do not regress.
- Independent consumers use the V2 adapter: ticket, `a008.v2` WebSocket, command IDs, snapshot/inspect and no auto-resubmit after `COMMAND_UNKNOWN`/`SESSION_EXPIRED`.
- A008-0103 Stage 5 is Complete. Stage 6 is the Expo/native proof.

## Verification

- `npm run verify:client`: PASS.
- `npm run verify:protocol`: PASS.
- Focused client tests: 4/4.
- Final `npm test`: 711 core + 4 membership + 187 GUI, 0 failures/skips.
- Root typecheck/build: pass.
- GUI typecheck and production build: pass. Existing Zod annotation and >500 kB chunk warnings remain.
- `git diff --check`: pass.
- No live/paid provider call.

## Constraints kept

- No anonymous V2 on PIN-disabled hosts.
- No new V2 HTTP business routes.
- No V2 prompt attachment or V2 image-generation command.
- React is not a client-package dependency.

## Next action

After this PR integrates and is journaled on main, Stage 6 may start. Do not treat bundled V1 session chat as a V2 recovery surface. A later bounded child may add V2 attachment/image commands if the product GUI should leave the V1 adapter.
