# A008-0172 — Restore durable knowledge domain classification

Status: Complete
Date: 2026-09-24
Branch: `codex/a008-0172-domain-classification-regression`
Base claim commit: `cea6b35`
Implementation commit: `e4f7727d639d91c53939732963792ad829468f59`
Pull request: not opened

Worker completion is not operator acceptance. Do not treat this handoff as merged.

## Observed regression

`C:\log\a008-log.txt` was inspected locally. Across 16 non-empty
`knowledge_analysis` responses, only one emitted `domains`; that response
predated A008-0171. Later responses still emitted useful `tags` but omitted
`domains`, causing durable records to land in the Memory graph's Unlabelled
cluster despite functioning label persistence.

The runtime path was verified intact: intake normalizes `raw.domains` and
`live-commit` attaches staged domains to claims/utterances. The regression was
therefore in the dialogue extractor contract, not storage or graph rendering.

## Delivered

- Dialogue extraction now treats domains as broad reusable retrieval
  classification, not optional sparse metadata.
- Classifiable durable artifacts are instructed to emit the smallest useful
  domain set: normally one, at most two when genuinely cross-domain.
- Domains may be derived from the artifact subject even when the exact domain
  phrase is not quoted in the source.
- Tags and entities remain optional and sparse.
- Generic workflow labels remain discouraged.
- No runtime fallback/catch-all domain was added.
- State, relation classification, retrieval ranking, current scope, label
  persistence and source-ingestion extraction are unchanged.

## Verification

Fixture/local only; no live provider calls; 0 SEK.

- Focused semantic-json/extractor check: 33/33 passed.
- Combined extractor/intake/prompt checks: 60/60 passed.
- TypeScript typecheck: passed.
- `git diff --check`: passed before closure.

## Documentation

- `docs/CURRENT_MEMORY_MODEL.md` now distinguishes broad domain
  classification from sparse tags/entities.
- `docs/SYSTEMDOC.md` records the implemented dialogue-extraction rule.
- `docs/tasks/README.md` indexes A008-0172 and its archive.
- `docs/CURRENT_STATUS.md` is intentionally unchanged until integration because
  it records merged observed reality.
- `docs/JOURNAL.md` is intentionally left for the operator merge step.

## Follow-up boundary

Existing unlabeled records were not retroactively reclassified. If desired,
that should be a separate migration/reclassification task with its own source
and provenance rules rather than an implicit runtime fallback.
