# Handoff — A008-0111

Status: Complete, ready for operator integration
Task ID: A008-0111
Branch: `codex/A008-0111-existing-project-registration`

## Outcome

Projects now has a distinct `Add existing` path beside New and Recent. An existing absolute directory is canonicalized, assigned a generated A008 project ID, registered outside the project tree and opened through the existing shared workspace/runtime owner.

Registration never initializes Git or writes/repairs files inside the selected project. Duplicate roots and invalid paths fail closed. Existing Docs-First markers may be observed read-only. The explicit memory toggle uses the new registered namespace; legacy/unregistered memory is not guessed, merged or migrated.

V1 adds typed `POST /v1/projects/register`; protocol route/schema/OpenAPI ownership is updated.

## Verification

- Full `npm test`: 593 core, 4 membership, 162 GUI; all passed.
- Focused service/host registration suite: 11/11 passed.
- Installed/offline `@a008/protocol` consumer verification passed.
- Production GUI build and `git diff --check` passed.
- No live provider call, deployment or running-host restart.

## Continuation

A008-0103 remains paused at the same stage boundary. This task supplies the owner-requested prerequisite; it does not advance the V2 program.
