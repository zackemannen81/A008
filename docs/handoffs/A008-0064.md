# A008-0064 — Memory GUI handoff

Task ID: A008-0064
Status: Complete
Branch: `codex/A008-0064-gui-memory-diagnostics`
Head commit SHA: `6bed751bc706b29b96e6dbdc982a95c3fccebc9d` (implementation;
followed by the archive/template-restoration documentation commit)
Pull request: none; push and publication were not authorized.

## Result

The existing A008 diagnostic GUI has Memory Overview, Memory Relationship Map
Graph and Memory Knowledge Manager under Memory in the workspace header.
Data comes from the chat ACP runtime through read-only `/v1/memory` ->
`memory/inspect`. No separate store, provider invocation or mutation is added.
Chat/session/draft survive page navigation. Refresh is explicit.

## Changed files

- `gui/src/memory/` (client, three views, inspector, CSS, tests), `gui/src/app.tsx`
  and the shell's navigation/layout CSS; `gui/README.md`.
- `src/memory/knowledge/inspection.ts`, runtime and ACP/host wiring;
  `test/memory-inspection.test.ts` and root test-suite membership.
- ADR 0025, host contract, current status/system/file map, charter, archive,
  evidence/screenshots and collection indexes. Task ID was claimed on main first.
- The owner's untracked `docs/concepts_sandbox/gui/` remains untouched and excluded.

## Verification

`npm test`: 437 core, 4 repeated membership, 80 GUI; all passed.
`npm run typecheck`, `npm --prefix gui run typecheck`,
`npm --prefix gui run build`: passed.
`node --test dist/test/memory-inspection.test.js`: 8 passed.
Scoped docs link/fence, secret-pattern and diff checks passed.
CURRENT_TASK is restored from its template in the closure commit.

[Browser and integration evidence](../evidence/A008-0064_memory-gui-proof.md)
covers real host/ACP/SQLite, provider-free reads, escaped details, filters,
pagination, graph keyboard selection/zoom, empty/error/retry, preserved chat/draft
and 390px layout. Test hosts were stopped and ports 19864–19866 are free.

## Limits and skipped gates

Inventory scans the local namespace; graph caps are 80 records/240 links and
previews cap details at 16,000 characters. The manager is read-only. No live
provider test, paid call, desktop/package installation, push or deployment.
No JOURNAL entry before integration; the operator appends it on merge.

## Next action

No implementation blockers. Review and integrate the local branch when desired.
Restart the GUI host after building (`npm run gui`) and open **Memory**.
The normal host configuration is needed even though inspection makes no model call.
