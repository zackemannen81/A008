# Handoff: A008-0023

Status: Ready for operator merge review

Task ID: A008-0023
Parent: A008-0021
Worker: A008-worker01
Branch: `grok/A008-0023-repair-direct-match-eligibility`
Base: `origin/main` (`55e38f4`)
Head SHA: 095f0cbcec2264a3f06b7c5eea89267c9060f2c8
Pull request: https://github.com/zackemannen81/A008/pull/2

Do not merge from this worker. Operator is the only merger to `main`.

## Files changed

- `docs/CURRENT_TASK.md`
- `docs/HYBRID_MEMORY_READ_PATH.md`
- `docs/SEMANTIC_MEMORY.md`
- `docs/handoffs/A008-0023.md`
- `docs/handoffs/README.md`
- `src/acp/a007-acp-agent.ts` → `src/acp/A008-acp-agent.ts` (rename only)
- `src/memory/hybrid-memory-reader.ts`
- `src/memory/hybrid-retrieval-policy.ts`
- `src/memory/memory-engine.ts`
- `test/hybrid-memory-reader.test.ts`
- `test/memory-engine.test.ts`

Not changed: `src/memory/types.ts`. Not created: `src/memory/knowledge/`.

## Behavior

- Exact/direct channel hits ignore activation for projection eligibility.
  Included dormant exact hits record reason `direct_match_ignores_activation`.
- Associative (non-exact) dormant hits are omitted with
  `associative_activation_dormant`.
- Exact-channel scoring uses `weights.strength = 0`. Associative hits still
  use stored strength.
- `SemanticMemory.project()` is read-only: no reinforcement, activation,
  revision, or audit write. `projectSelected` admits ranked/required current
  records without an activation gate. `required_not_eligible` does not fire
  for a dormant direct/required match.
- Write-path `reconciliationReinforcement` remains the named explicit
  reinforcement call.

## Exact verification commands and results

Node v24.14.1. Date 2026-09-02. Clone `C:\code\A008-workers\A008-worker01`.

### `npm run typecheck`

```text
> A008@0.0.0 typecheck
> tsc -p tsconfig.json --noEmit
```

Exit 0.

### `npm test`

```text
ℹ tests 167
ℹ pass 167
ℹ fail 0
ℹ duration_ms 9287.14
```

Exit 0.

Focused S6/S7 reduced tests (all pass):

- `S6 reduced: dormant exact/direct hit is projected without mutation`
- `S7 reduced: dormant associative hit is omitted with an explicit reason`
- `S6 reduced: a dormant current record is returned for a direct question`
- `exact-channel scoring zeros memory strength; associative scoring keeps it`

### `git diff --check`

No output, exit 0.

### Other gates

- No `KnowledgeItem` field added (`src/memory/types.ts` untouched).
- No live provider call, `.env.local` read, or OpenHands mutation.

## Skipped gates and reasons

None of the frozen charter gates were skipped.

An initial `npm test` in the agent environment failed
`compiled ACP process completes one turn through a local fake NVIDIA endpoint`
because the child inherited both `NO_COLOR` and `FORCE_COLOR` and Node
printed a warning to stderr. Re-running without those variables was green.
That failure is environmental, not a product defect, and is outside this
charter.

## Unblocking rename

`origin/main` imported `./A008-acp-agent.js` while the blob was still
`src/acp/a007-acp-agent.ts`, so `npm run typecheck` could not run. The file
was renamed to the documented A008 path (100% similar). No ACP behavior
change. Product identity is A008.

## Blockers

None for this child. Operator merge to `main` is intentionally not done.

## Next recommended operator action

1. Review https://github.com/zackemannen81/A008/pull/2
2. Merge only after the frozen gates and this handoff
3. Continue A008-0021: wave-1 children may land in any order; then M4/M5
