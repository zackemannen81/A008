# A008-0096 Handoff

Status: Post-review hardening implemented; local verification required before merge

## Delivered

- Removed the regex mini-parser from the live knowledge commit path.
- Preserved validated structured claim propositions from staging; retained explicit `statement` fallback for unstructured claims.
- Made user-assertion acceptance support-span aware so mixed request/assertion utterances are handled per evidence span.
- Prevented request/imperative evidence from becoming user-asserted world state.
- Prevented full proposition text from contaminating entity labels, including the no-entity fallback case.
- Derived claim certainty from staged confidence rather than a single hardcoded certainty.
- Moved the four-domain cap to aggregated utterance labels; proposal/claim domains remain intact.
- Retained duplicate-edge suppression in inspection while preserving provenance records/nodes.
- Added focused A008-0096 regression coverage and wired it into the core test command.

## Verification

The earlier implementation passed typecheck and the full repository suite before these post-review changes. The new review fixes were committed through the GitHub connector, where no CI workflow is configured and no executable workspace is available.

Run before merge:

```text
npm run typecheck
npm test
git diff --check origin/main...HEAD
```

If any check fails, keep PR #41 open and repair on `a008/A008-0096`; do not merge a partially verified task.

## Next Step

Pull the latest `a008/A008-0096`, run the three verification commands above, review the resulting diff, then update this handoff/archive with the exact fresh results before merge.
