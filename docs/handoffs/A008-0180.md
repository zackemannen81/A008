# A008-0180 Handoff

## Completed

- Added a regression invoking the installed `agent-browser` MCP command through A008's real stdio probe.
- Verified initialization, `tools/list`, schema validation, and close with a non-empty core catalog.

## Verification

- `node --test dist/test/model-tools.test.js` — 16 passed.
- `npm run typecheck` passed.
- `npm --prefix gui run test` — 205 passed.
- `git diff --check` passed.
- No live provider calls; 0 SEK.

## Result

`agent-browser` 0.38.1 starts successfully in this environment and reports 29 tools. The reported first-call failure could not be reproduced in A008's MCP subprocess boundary. The external A008 Client MCP-launch timeout remains outside this task's proven scope.

## Follow-up

Capture the external client's startup error, process command, environment, and timeout boundary before claiming an integration repair task.