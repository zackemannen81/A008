# A008-0179 Handoff

## Completed

- Worktree bridges now reuse the existing registered runtime by project ID.
- Engine sessions retain their requested workspace CWD for tools and snapshots.
- The GUI creates and opens an isolated session in one action.

## Verification

- `npm run typecheck`
- `npm run build --silent && node --test dist/test/local-acp-bridge.test.js` — 4 passed.
- `npm --prefix gui run test` — 205 passed.

## Documentation follow-up

`docs/CURRENT_STATUS.md` and `docs/SYSTEMDOC.md` were locked by another process during this run. Update their worktree sections with this behavior: opening a worktree resolves the existing registered project runtime by project ID; `EngineHost` retains the selected worktree CWD for ModelToolSession and runtime snapshots; the GUI creates and opens a parallel session in one action.

## Limitations

Merge, pull-request creation, push, remote mutation, and branch deletion remain unavailable.
