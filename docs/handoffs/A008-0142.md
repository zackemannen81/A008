# A008-0142 Handoff — In-sequence image generation and model-triggered image tool

Status: Complete
Task: A008-0142
Parent Task: none
Branch: `A008/A008-0142_UX_ImageGenerating`
Implementation base: `baef53a6b122a3ad177c3ae4e5a471027be39676`
Pull request: open after push

## Delivered

- Generated images are canonical ordered conversation content (`generated_image` parts) with string messages kept as a compatibility form.
- Manual `+` → Generate image and structured `generate_image` both reserve one `[IMAGE GENERATING]` item immediately, then resolve that same item in place.
- Completion, failure and cancellation stay at the reserved transcript position. Later text can continue around a pending image. Reverse completion order cannot reorder or duplicate items.
- Successful provider output remains a source-store locator. Provider job IDs, credentials and temporary URLs do not enter the model tool result or durable chat state.
- GUI reconstructs image turns from the host snapshot. Same-process reconnect keeps pending/completed items; a new process does not replay a lost chargeable generation.
- ACME remains the chat execution substrate and is not the media-generation owner.

## Verification

- `npm run typecheck`: pass.
- `npm test`: 701 core + 4 membership + 186 GUI = 891 pass, 0 failures/skips.
- `npm --prefix gui run typecheck`: pass.
- `npm --prefix gui run test`: 186 pass, 0 failures/skips.
- `npm --prefix gui run build`: pass. Existing warnings only: two Zod annotation comments and a >500 kB minified chunk.
- `npm run verify:protocol`: pass.
- `git diff --check`: pass.
- No live/paid provider call. Manual GUI proof used deterministic/fake provider plumbing.

## Boundaries

- No Kie webhook/callback replacement.
- No image editing, galleries, batch generation or ACME media ownership.
- No Stage-5 SDK/web migration.
- V1 in-flight image jobs remain process-local.

## Next action

Operator inspects the branch, journals on merge to `main`, and opens the pull request. Do not merge from this worker session. Workers do not append `docs/JOURNAL.md`.
