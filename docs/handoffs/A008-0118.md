# Handoff — A008-0118

Status: Complete; Stage 3.5 **GO**
Task ID: A008-0118
Parent: A008-0103
Branch: `grok/A008-0118-acme-runtime-v2-consumer`

## Outcome

A008 consumes `acme-model-runtime/2` through `AcmeChatTransport`. Selection is
explicit (`A008_CHAT_TRANSPORT=acme`, `A008_ACME_MODEL_RUNTIME_URL`, bearer
token, optional pinned `A008_ACME_ENGINE_BUILD`). ACME routes only the
`executionProvider` A008 already chose. Direct NVIDIA/OpenAI/KIE chat
transports remain available as reference. There is no post-dispatch fallback.
Image/audio/video stay on their current owners.

Stage 3.5 adoption decision: **GO**.

## Verification

- Offline: 636 core, 4 membership, 162 GUI; typecheck; protocol package;
  production GUI build; `git diff --check`.
- Live chat PASS: Luna, Nemotron 3.5 Lightning, Kimi K3, DeepSeek V4 Pro,
  Muse Glimmer, Laguna XS, KIE/Gemini. Engine build `acme-0180-local-a008`.
- Owner client loop PASS on two models: chat → tool → continuation →
  retrieval → answer → extraction → one batch relation → commit → next turn
  retrieves memory.
- Image generation still works on the existing NVIDIA/KIE image path.
- Nemotron Omni was not live-tested; it was not in the frozen named order.
- Owner screenshots: [evidence record](../evidence/A008-0118_acme-runtime-v2-go.md).
  Muse answers the `oldschool` neon-text question from stored `demo.js`
  knowledge. Luna and DeepSeek still generate images. Code Canvas preview
  still renders.

## Continuation

A008-0103 Stage 4 is next: turn/message identity, snapshot/event boundary,
terminal outcomes, command receipts/idempotency, reconnect/resume. Do not
conflate those application identities with ACME `modelExecutionId`.
