# Handoff â€” A008-0112

Status: Complete; Stage 3 complete
Task ID: A008-0112
Parent: A008-0103
Branch: `codex/A008-0112-v2-session-transport`

## Outcome

V2 is now a usable authenticated session transport. `/v2/session` negotiates `a008.v2`, consumes a one-use first-frame ticket, binds principal/project/session authority and dispatches new/inspect/prompt/cancel/control/tool-permission through the shared ProjectRuntimeRegistry/EngineHost. Device capability/expiry is rechecked per operation; revoke or expiry closes the live socket, cancels owned work and denies pending permissions.

Shared `@a008/protocol` schemas describe V2 auth, commands, results, signals and errors. Pre-auth/authenticated bounds are 4 KiB/1 MiB, authentication deadline is five seconds, prompt input is 64 KiB, and configured secrets pass the existing wire-redaction boundary. V1/ACP behavior remains compatible.

## Verification

- Focused real-host V2 suite: 13/13.
- Final full root gate: 602 core + 4 membership + 162 GUI = 768, zero failures/skips.
- Installed/offline protocol consumer, generated schemas, production GUI build and portable-engine V2 discovery proof pass.
- No live provider call, real owner credential, deployment or host restart.

## Continuation

A008-0103 Stage 3 is complete. Stage 4 is next: stable turn/message IDs, authoritative snapshot/event boundary, terminal outcomes, command IDs/idempotency receipts, reconnect/resume and restart/cancellation recovery semantics. Do not make Tauri/Expo invent those independently.
