# A008-0156 — Platform specification

Status: Complete
Date: 2026-09-22
Branch: `codex/a008-0156-platform-spec`
Source baseline: `77060079ff85635b6b4fe888f01038aedbb4c41a`
Task allocation: `544d23b` on main, cherry-picked as `bffdddd` on this branch.
Head at handoff preparation: `bffdddd`; final documentation commit is identified
by subject `docs: specify distributed A008 platform (A008-0156)`.

## Deliverable

[A008 Platform](../A008_PLATFORM_SPEC.md), Swedish review draft v0.9:
28 sections, 12 requirements, 24 future acceptance scenarios and 12 decisions
with recommendations. The owner's original topics have a coverage map.

The core addition is explicit backend-owned parallel background work, including
per-conversation writes, workspace isolation, durable receipts/outbox, worker
leases, external-effect uncertainty, cancellation and multi-client recovery.
The draft also covers identity, devices, memory, secrets, admin, clients,
operations, migration and a staged route to the first two-client proof.

## Authority and compatibility

The owner selected the platform direction. Detailed requirements remain proposed;
no accepted ADR, API, memory model or product implementation was replaced.
Today's V2 interrupts work on disconnect and loses recovery authority at restart.
ADR 0043 prohibits ACME tool execution. Durable memory-off conversations are also
a proposed change. All three differences have explicit migration/decision notes.

Resolve the P0 decisions before implementation. D4 recommends retaining the
current A008 tool owner for the first proof and considering a bounded ACME
tool-execution amendment separately.

## Verification

- Document structure/ID/coverage checks pass: 28 sections, 12/12 requirements
  mapped to 24 acceptance scenarios, 12 decisions, 13 resolving local links,
  balanced fences and valid JSON.
- Current-versus-target and concurrency/failure/isolation/memory/migration
  walkthrough completed. Mermaid diagrams reviewed as text, not browser-rendered.
- Diff/secret review and whitespace check pass; no product source changed.
- Runtime tests, build, live providers and platform acceptance execution were
  not run: this is a documentation task, not evidence of implemented guarantees.
- Final closure verifies collection links, archive Status, CURRENT_TASK template
  equality and documentation-only staged paths.

## Integration and next step

The branch includes the prior sidebar work/checkpoint as its baseline; inspect
the A008-0156 commit separately if integrating onto another branch.
Main has the ID allocation only. No push, merge, deployment or publication.
Operator appends JOURNAL on integration.

Recommended next task after P0: durable background runs for two isolated
projects using existing A008 cognition and ACME model execution. Mobile, TV,
full admin UI and ACME tool migration are later bounded work.

Archive: [completed task](../finished/A008-0156_platform-specification.md).
