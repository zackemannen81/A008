# A008-0110 handoff

Task ID: A008-0110
Status: Complete; merged; stopped at owner-requested boundary
Branch: codex/A008-0110-v2-auth

Shared V2 auth/discovery/error schemas and generated OpenAPI are implemented.
Local device grant/list/revoke stores only hashes and scoped metadata outside
the repo; default expiry is 30 days. Real HTTP discovery/ticket endpoints enforce
PIN or device auth, project/capability scope, Origin, expiry and one-use tickets.
Only auth.tickets is advertised. V1/panels retain their existing behavior.

756 tests, root/GUI builds, installed protocol and portable engine proof pass.
GUI warnings remain (533.97 kB main JS, gzip 161.63 kB). No live provider calls,
real device grants or running-host restart. Existing services may run older code.

## Stop and resumption

The owner requested stopping at the next suitable boundary because the session
limit was low. Stop after this task's merge and operator journal; no new child
is allocated. Parent A008-0103 remains In Progress: stages 1 and 2 complete,
stage 3 partially implemented, stages 4-7 unstarted.

Next operator: read CURRENT_TASK/template and authority docs, then ADR 0041,
CLIENT_API_V2.md, CLIENT_AUTH.md and the parent program. Allocate the next ID on
main and freeze the next stage-3 task before edits. Wire V2 first-frame ticket
admission, project/session ownership and authenticated business dispatch. Do not
claim native or full V2 support from this foundation. Pending security gates:
5-second/4-KiB pre-auth bounds, per-operation capabilities/expiry, live revoke
closing sockets/cancelling work/denying approvals, project/session mismatch and
real-host tests. Then implement stage-4 snapshots/turn outcomes/receipts/recovery,
stage-5 SDK and full web migration, actual Expo/external-platform proof, and the
compatibility release gate. No durable chat/background-run survival is adopted.

Standing authorization permits per-task push, PR, merge and journal updates;
the explicit stop controls this run. No restart was performed in tasks 0106-0110.

Integration: PR #51 merged as 138bf30 on 2026-09-15 local time.
Operator journal is updated; no next task was started.
