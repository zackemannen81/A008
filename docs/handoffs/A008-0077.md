# A008-0077 handoff

ChatGPT and NVIDIA Build refuse iframes (`frame-ancestors`). The Browser pane
now checks that before framing. Blocked sites show an explanation and
**Open in browser**. The address bar also has **Open** for every URL.

A008 cannot force those sites into an iframe. A desktop WebView2/BrowserView
as a top-level document would be a later change.

Verification: [evidence](../evidence/A008-0077_browser-frame.md).
