# A008-0081 — Evidence lifecycle (L2)

Status: Complete
Date: 2026-09-08
Operator: Codex
Branch: `codex/a008-0081-evidence-lifecycle`
Implementation head: `e7f4874b9ee4616b7b029f54d0d1ae43b225fea0`
Pull request: none; owner authorized direct main integration/publication.
Blockers: none.

## Result

L2 implements accepted ADR 0035 P1-P5: per-claim severity, persisted creation
policy, lazy exponential evaluation and source-supported exact-target boosts.
An occurrence/claim receipt commits with evidence, baseline and audit under the
existing SQLite transaction owner. Restatement reuses canonical evidence and
creation occurrence dedup prevents retries from producing fresh copies. Source
attribution, state/history ownership and direct dormant eligibility survive.

Knowledge schema 3 converts legacy baselines at one migration time, preserves
thresholds/pins/history and rejects incompatible older binaries. Settings format
3 retains advanced creation policy through operation snapshots and old-client
saves. Reads and inspection never reinforce; baseline and evaluated numbers
are distinct in inspection and absent from model projections.

## Files changed

The implementation commit contains the exact 46-file list. Production owners
are core runtime preferences/policy; knowledge lifecycle, transaction, SQLite,
read/inspection and live commit; analyzer/staging/comparator serialization and
local runtime composition. The benchmark and existing test fixtures supply the
required analyzer enum; package.json only adds the L2 test to core membership.
New acceptance coverage is in
[lifecycle-v1.test.ts](../../test/knowledge-model/lifecycle-v1.test.ts).
Owning docs, indexes and the
[completed charter](../finished/A008-0081_evidence-lifecycle.md) describe actual
behavior and map A09-A24, claim-side A29 and A30 to executed evidence.

## Verification

- `npm run typecheck` and `npm run build`: passed.
- `npm run test:core`: 511 passed; 0 failures/skips, including 10 L2 groups.
- `npm run test:membership`: 4 passed.
- `npm run test:gui`: 116 passed; 0 failures/skips.
- Fixed-clock and in-memory/SQLite parity; source proof and relation matrix;
  duplicate creation/receipt, cancellation, failure rollback, concurrent
  connection delivery and restart; legacy migration rollback/retry, prior
  published store execution/rejection and SQLite backup restoration.
- Final necessity review: no L3 metadata, new ranking, domain redesign,
  background sweep, model calls/dependencies or L1 instruction changes.
- Before handoff: 10 changed Markdown files/132 links; no new path/anchor/fence
  failures. Frozen specification body and charter, existing immutable records,
  template bytes, UTF-8, credential patterns and staged diff checks passed.
  The integration review also checks this handoff and the operator journal.

Skipped: live/paid model judgments, physical power-loss testing, migration of
user data, running-application restart, packaged distribution rebuild and
deployment. Synthetic semantic decisions do not prove live model judgment.

## Operator continuation

Identity `28bebb3` and frozen charter `a6ed013` were published on main before
implementation. Fast-forward main, publish, journal the integration and verify
remote/local equality. CURRENT_TASK is restored byte-for-byte. The pre-existing
untracked `.grok/` directory was not read, modified or staged.

The specification body remains frozen. L3 requires its own activated charter.
Before opening valuable data with the new build, follow the constitution's
[backup and restore procedure](../KNOWLEDGE_MEMORY_MODEL.md#12-storage-deliberately-deferred).
