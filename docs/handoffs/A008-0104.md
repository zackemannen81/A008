# A008-0104 handoff

Task ID: A008-0104
Parent: A008-0103
Status: Complete locally; not pushed or merged remotely
Branch: codex/A008-0104-v1-contract
Allocation on main: 26e1609
Frozen charter/program: 31c7875

The owner approved freezing the reviewed plan, chartering/freezing the first
task and beginning implementation. ADR 0040 adopts the unchanged plan body with
its SHA-256. A008-0103 tracks the seven-stage program; this first child establishes
the shared v1 session/transport foundation and inventories the full surface.

`packages/protocol` owns existing WS messages, session snapshots/controls,
generation metadata and runtime preference wire shapes. Host/core/GUI adapters
consume the same owner. Root build and portable engine ship compiled protocol
modules; the separate package installs independently. Runtime policy, auth,
memory ownership and current v1 tolerance/error behavior are preserved.

Verification: 90 frozen legacy cases; 573 core, 4 membership and 161 GUI tests;
real host frames/model responses; five generated-schema drift checks; platform
import boundary and dispatcher/route inventory checks. Root build, GUI typecheck
and production build pass. The offline installed package consumer compiles/runs.
The portable engine passes discovery, ACP/panel history, explicit tool approval,
actual fixture command/result, upload/memory, disconnect and graceful stop.
All test conversations and provider replies were synthetic. The existing
downstream client clone is only a compatibility fixture and was not modified.

GUI build warnings remain: 526.22 kB main JS (159.20 kB gzip), above Rollup's
500 kB warning threshold, and removed Zod PURE-comment annotations. Shared Zod
validation adds GUI weight; this task does not add unrelated chunk splitting.
Structural JSON schemas do not encode runtime-budget cross-field constraints;
use the shared validators/parsers and package documentation for v1 semantics.

The [inventory](../HOST_PROTOCOL_V1_INVENTORY.md) confirms a next stage-1 child is
needed for HTTP payload schemas outside the session/model/settings foundation.
Freeze that child under the existing program before implementation. Stage 1
remains In Progress. V2/auth, project ownership, recovery guarantees, SDK/web
migration and the independent Expo/platform proof are still pending.

Final scope/necessity review, changed-document links/fences, frozen plan hash,
diff and template-identity checks pass. The completed charter is archived under
`docs/finished/A008-0104_shared-v1-contract.md`; CURRENT_TASK is the empty template.
No provider charges, running-host restart or remote publication accompanied this
build. Operator integration/journal and any remote push/PR/merge remain separate
actions; no extra runtime behavior should be inferred from a local commit.
