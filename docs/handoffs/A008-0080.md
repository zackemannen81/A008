# A008-0080 — Coherent chat instruction

Status: Complete
Date: 2026-09-08
Operator: Codex
Branch: `codex/A008-0080-coherent-instruction`
Implementation head: `ea158bf52a9a02e2f4c1c4aac645028a88772393`
Pull request: none; owner authorized direct main publication/integration.

## Result

L1 of [the frozen specification](../backlog/instruction-plane-and-memory-lifecycle.md)
is implemented. Shared core composition emits one system message containing
explicit session/global instructions and the applicable memory rule. Generic
fallback is selected only when explicit configuration is absent. Runtime
factories and CLI preserve that provenance; an explicit fallback-equivalent
string survives. Tool rounds use one settings snapshot.

The default is generated for the outgoing request instead of becoming synthetic
history. Explicit session-base metadata remains supported. Envelope serialization,
retrieval, knowledge lifecycle, schemas and policy settings are unchanged.

## Evidence and scope

- [Completed charter](../finished/A008-0080_coherent-instruction.md) maps A01–A08
  to executed fixtures and names the live-behavior limitation.
- `npm run typecheck`, `npm run build`: passed.
- Targeted instruction/runtime/CLI/provider/semantic tests: 60 passed.
- `npm run test:core`: 501 passed, 0 failed/skipped.
- `npm run test:membership`: 4 passed.
- `npm run test:gui`: 116 passed, 0 failed/skipped.
- Includes real local GUI host/ACP/engine paths, captured NVIDIA/kie HTTP
  payloads, external preference saves during tool execution, Unicode byte-budget
  boundaries and original-message/final-answer intake.
- Documentation check before handoff: 7 Markdown files/79 links, no new
  reference/fence failures. Frozen specification hash and charter, previous
  immutable records, template bytes and diff checks passed; credential-pattern
  scan found no matches. Final checks add this handoff and operator journal.
- Production changes: CLI, core invocation/session, memory-aware session/prompt,
  local runtime and NVIDIA session factory. Tests change only relevant existing
  files. Documentation updates current behavior, file map, settings and task state.
- Skipped: live/paid provider behavior, packaging rebuild, deployment/release.
  Offline wire tests do not establish model obedience. No dependency/package
  or platform changes require a new distribution build.

## Integration and continuation

Identity `2f2275c` and approved specification/Ready charter `b9cfe1a` were
published on main before implementation. The specification body remains pinned
by ADR 0035; P1–P6 are accepted target choices, so no repeated approval of those
same choices is needed. L2/L3 remain separate, unactivated implementation tasks.

The operator fast-forwards main, appends JOURNAL, pushes and verifies remote
equality. CURRENT_TASK is already restored byte-for-byte from its template.
Existing untracked `.grok/` is outside this task and was not read or staged.
