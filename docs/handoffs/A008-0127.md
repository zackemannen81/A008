# A008-0127 Handoff

Status: Complete
Owner: ChatGPT (operator)
Completed: 2026-09-18

## Integration Identity

- Branch: `chatgpt/A008-0127-embedded-acme`
- Reviewed implementation/completion head: `082bdc2118e64eb183b433000bc696b219df623c`
- Pull request URL: Pending operator creation after final handoff push.
- Dependency release: `acme-engine@0.1.3` -> `@acme-engine/model-runtime@0.1.3` -> `@acme-engine/adapter-model-chat-completions@0.1.3`.

## Delivered

- Embedded ACME is the default A008 chat execution path; normal startup no longer requires an ACME sidecar URL, token, build pin or separately started process.
- A008 remains authoritative for cognition, memory, prompts, tools, model selection, provider strategy, credentials and endpoints.
- `A008_CHAT_TRANSPORT=direct` preserves the direct reference/debug path; `A008_CHAT_TRANSPORT=acme` preserves the remote sidecar compatibility path.
- Embedded runtime composition derives from A008 model metadata and user catalog and rebuilds after catalog changes.
- Existing request/result/error/evidence mapping is reused for in-process execution; tool continuation, streaming callbacks, errors and `modelExecutionId` evidence remain intact.
- Invocation-local images map to ACME vision parts/capability requirements while durable conversation history remains text-only.
- A008 now declares Node `>=24.0.0 <25`, matching the published ACME facade contract.

## Verification

- `npm run test:core` — 657/657 PASS.
- `npm run test:membership` — 4/4 PASS.
- `npm run test:gui` — 165/165 PASS.
- `npm run verify:protocol` — PASS, including packed protocol/dependency install and independent TypeScript consumer.
- `npm run typecheck` — PASS.
- `npm --prefix gui run typecheck` — PASS.
- `npm --prefix gui run build` — PASS; only existing Rollup/Zod annotation and chunk-size warnings.
- `npm run format:check` — PASS.
- Scoped ESLint on changed embedded/runtime source — zero errors; the task test file is excluded by the repository ignore pattern.
- `git diff --check` — PASS.
- Focused A008-0127/parity/config suite — 20/20 PASS before full-suite verification.
- Live text smoke forced `A008_CHAT_TRANSPORT=embedded-acme` with isolated SQLite state and Nemotron 3.5 Lightning; committed history contained exactly `assistant: A008-0127-EMBEDDED-OK`.
- Vision live attempts were bounded but provider-unstable: one produced no response and was operator-terminated after ~185 s; one returned structured ACME `TIMEOUT` after ~94 s. Offline vision mapping/capability/history regressions pass; no live vision success is claimed.

## Skipped / Known Non-Gates

- Repository-wide lint is not a task completion gate and still has pre-existing errors outside A008-0127; changed source is scoped-lint clean.
- No further live vision calls were made after the two bounded provider failures.

## Operator Action

Merge ACME-0185 first, then ACME-0186. After those source-history PRs are integrated, open and merge this A008 branch. The A008 branch itself is ready for owner GUI testing now with `npm run gui`; no ACME sidecar should be started.
