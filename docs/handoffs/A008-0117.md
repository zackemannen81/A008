# A008-0117 handoff

Retrieval now has an explicit semantic necessity decision. Successful `retrieve=false` classifications skip knowledge reads and projection entirely. Successful retrieval classifications are bounded to narrow direct/related labels. Longer lexical queries require two content-token overlaps, preventing a generic word such as `demo` from pulling project-wide history. Classifier failures remain fail-open as deterministic retrieval and are reported as `degraded`; intentional skips are reported as `skipped`. Verification: 632/632 core, typecheck and diff-check pass. ACME remains the selected execution route for semantic `retrieval_scope` when configured.
