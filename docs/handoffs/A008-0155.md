# A008-0155 handoff

Status: Complete
Date: 2026-09-22
Owner: Codex (operator)
Branch: `codex/a008-0155-project-sidebar`

The owner-requested Projects sidebar now shows registered folders and their
saved chats, with collapse, open and New chat actions. Its ellipsis/right-click
details dialog shows path/count, supports pinning and display-name edits, uses
the active theme and returns keyboard focus on Escape. Names sort alphabetically
after pinned entries. The list scrolls separately from navigation/footer; mobile
navigation and dialog sizing were checked at 390×844.

The existing SQLite conversation owner now stores multiple chats with an active
selection. Legacy current content migrates transactionally and is retained;
new chat does not erase earlier chats. Reset/model change replace only the
selected chat. Host-owned summary/update/chat routes and independent SDK helpers
reuse the existing registry/runtime/session owners. No provider call is needed.

## Verification

- `npm test`: 728 core + 4 membership + 192 GUI = 924 pass, 0 failures/skips.
- Root typecheck/build and GUI production build pass.
- Generated protocol schema, route inventory, packed protocol and packed client
  independent offline checks pass.
- Real production GUI/host with temporary synthetic data: saved chat → new chat
  → earlier chat, rename, pin, collapse, keyboard focus, all three themes and
  desktop/mobile checks pass. No page errors or horizontal overflow.
- Final necessity review and `git diff --check` pass.
- Existing Zod annotation and >500 kB bundle warnings remain.

## Integration

- [Completed charter](../finished/A008-0155_project-sidebar.md).
- [Decision](../adr/0047-project-sidebar-and-saved-chats.md).
- Current-task file restored byte-for-byte from its template.
- No push, deployment, paid/live-provider call or publication performed.
- Operator appends the journal on merge. No unresolved implementation blocker.
- Pre-existing logo modifications and `.tmp-agent-browser-isolation.mjs` remain
  outside this task and must not be included in its commit.

Memory-disabled projects retain chats only in the running host. The existing
global standalone workspace selection and no-concurrent-writer limitation remain;
generic ACP/V2 sessions do not become durable or acquire new execution authority.
