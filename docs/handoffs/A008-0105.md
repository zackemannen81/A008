# A008-0105 handoff

Task ID: A008-0105
Parent: A008-0103
Status: Complete locally; not pushed or merged remotely
Branch: codex/A008-0105-http-contracts
Base: remote main c3970d5 (PR #45 verified merged)
Allocation on main: 247ef82
Frozen charter: db5fb2b

Shared HTTP contracts now cover memory, project bootstrap/open/browse, provider
catalog/settings/images, upload/blob, shell, frame checks and health/login/errors.
Host/core/GUI DTOs use `packages/protocol`; runtime normalization, filesystem and
provider policy retain their owners. Existing pure client parsers move to the
package. Previously casting adapters keep their existing behavior.

`http-operations.ts` maps every inventory row to its payload contracts; 35 schema
components generate `schemas/http.openapi.json` (OpenAPI 3.1.1). This is a shipped
description, not a new endpoint or V2 guarantee. Binary media and HEAD remain
explicit. Raw accepted input and typed writer DTOs are distinguished for legacy
bootstrap/provider tolerance. Memory graph membership requires the shared semantic
validator beyond the structural artifact.

Verification: 52 captured pre-extraction HTTP parser cases and existing 90 WS
cases pass; a real loopback host exercises all 21 operation rows (22 methods),
PIN/origin/error paths, project mutations in temporary folders, actual inspection,
fake provider image bytes, shell fixture and upload extraction outcomes. Full
tests: 576 core, 4 membership, 161 GUI, zero failures/skips. Root/GUI build passes.
Independent offline tarball consumer compiles/runs HTTP/WS/OpenAPI; portable engine
discovery, ACP/panel/history, explicit approval, actual fixture command/result,
upload/memory and graceful stop pass. The downstream client is a read-only
compatibility fixture, never authority. No live provider or user-data mutation.

GUI main JS: 532.56 kB (161.06 kB gzip), with existing size/dependency annotation
warnings. Schema drift, operation coverage/references and an independent JSON
Schema 2020-12 compilation of all components pass. Final scope/necessity review,
documentation links/fences, frozen plan hash, diff, archive and template identity
pass. The archive remains immutable; the working tree's CURRENT_TASK is empty.

Next: the stage-1 V2 decision proposal, then accepted project/session ownership
and auth decisions before behavior changes. HTTP/WS extraction is complete but
V2, ownership/recovery redesign, SDK and Expo remain unimplemented. A008-0103
stays In Progress. Operator remote integration/journal and restart are separate;
this child performs no push, PR, merge, deployment or running-host restart.
