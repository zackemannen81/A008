# A008-0102 handoff

Task ID: A008-0102
Status: Complete locally — proposed plan, implementation not activated
Branch: codex/A008-0102-api-boundary-plan
Source/base commit: bbd4372

The owner requested a plan after the separately completed A008-0101 push, PR #44,
merge and host restart. The deliverable is
[host-client-api-boundary.md](../backlog/host-client-api-boundary.md), indexed in
the backlog. It records observed HTTP/WS, global standalone workspace, engine
project ownership, duplicate wire contracts and process-local recovery limits.

The proposed seven stages cover contract extraction, explicit project/session
ownership, V2/auth, turn/recovery semantics, SDK/web migration, an independent
Expo proof and a compatibility release gate. Existing project memory attachment,
ACP panels and v1 remain explicit migration obligations. Native background runs,
durable chat history, multi-tenancy and PostgreSQL are separate decisions.

Verification: read host/client parsers, host routing, engine runtime registry,
session DTO, auth/recovery adapters and owning protocol/ADR documents. The plan
distinguishes existing guarantees from proposals requiring ADR adoption. Technical
schema/transport references use primary specification/platform documentation.
No runtime code or dependencies changed; implementation tests are not applicable
to this docs-only slice. Changed-document links/fences, diff checks and current-task
template byte identity passed at finalization.

No hypothetical implementation task IDs were allocated. Next recommended slice:
inventory and consolidate the current v1 contract with fixtures and no behavior
change, then freeze the ownership/V2 decisions through the ordinary task workflow.
The plan is locally committed for review; it has not changed remote runtime behavior.
The completed planning charter uses the finished-task naming convention.
