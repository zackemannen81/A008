# A008-0122 Handoff

Status: Complete
Owner: ChatGPT (operator takeover)
Completed: 2026-09-17

## Delivered

- Distinct entity materialization with deterministic lexical identity and preferred display labels.
- Structural claim↔entity references persisted independently from L3 semantic associations.
- Current-batch entity handles available during relation classification.
- Structured propositions carried into proposal/candidate relation comparison and structured slot ownership.
- Shared self-contained single/batch relation prompt semantics; severity remains lifecycle-only.
- Read-only stored/effective/primary-effective domain projection for graph grouping.
- Assistant-answer-only claims now retain assistant provenance and cannot inherit user authority.
- SQLite schema 5 adds structural claim/entity membership; existing development stores may be deleted rather than migrated.

## Verification

- Focused A008-0122 regressions: 7/7 pass.
- `npm run test:core`: 644/644 pass, 0 failures/skips/cancellations/todo.
- `npm run typecheck`: pass.
- `npm run build`: pass.
- Final `git diff --check` passes after restoring `CURRENT_TASK` from the canonical template.

## Integration Notes

- `entity_ref` is structural inspection topology, not a semantic association. Do not attach L3 strength/decay to it.
- Relation classifier still owns semantic edges such as `requires`, `uses`, `depends_on`; structural membership only makes the involved referents addressable/navigable.
- Old alias-collapsed development stores are intentionally unsupported by migration/backfill; recreate them.
- The generated HTTP/OpenAPI memory record schema now permits `storedDomains`, `effectiveDomains`, and `primaryEffectiveDomain`.
- No GUI redesign is included; the existing memory UI can consume the repaired projection.

Blockers: none.
