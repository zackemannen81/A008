# A008-0083 — Semantic request repair

Status: Complete
Date: 2026-09-08
Operator: Codex
Branch: codex/semantic-request-repair
Integration: owner-authorized direct main publication.

Runtime semantic sampling now respects the existing fixed-top-P capability.
Kimi omits top_p, while the generic stateless generator preserves explicit null
and other models retain their semantic default. Relation instructions provide
canonical decision shapes and explicitly forbid input-envelope echoes.

Evidence: typecheck/build, 524 core tests, 116 GUI tests and 4 membership tests
pass without skips. The runtime/NVIDIA fixture exercises extraction and canonical
commit; the reported malformed JSON stays rejected in one call. No extra model
calls, parser repair, user settings/database changes or dependency additions.

Nine locally recorded HTTP 400 errors rejected top_p=1 for Kimi; a separate older
429 was rate limiting. The exact malformed response was not in the capped trace.
No private raw trace is published and no live model quality claim is made.

Next: A008-0084, the independently authorized memory map presentation repair.
The owner may verify live memory intake after starting the updated application.
