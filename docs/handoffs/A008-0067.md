# A008-0067 — Engine package handoff

Task ID: A008-0067
Status: Complete locally; integration pending
Branch: `codex/A008-0067-engine-package`
Head commit SHA (implementation): `649a32db3ef913271c0c0defa5125ed93f049430`
Companion branch/implementation: `codex/A008-0067-engine-panels` / `5b0abb2`
Pull request URL: https://github.com/zackemannen81/A008/pull/22

## Result

The portable A008 engine retains all existing Chat/Memory/Tools/Parameters views.
The companion client hosts them in a generic panel attached to its actual ACP
session. The engine owns project runtimes, global settings, upload and semantic
memory. Structured provider tools execute through validated native shell or
approved stdio MCP, with native per-action permissions, cancellation and editable
bounds. Actual results return to the model; optional prompt text is not bundled,
auto-loaded or hardcoded. No frontend implementation was copied across repos.

See [ENGINE.md](../ENGINE.md) for launch/discovery, data attachment and client
requirements, [settings](../RUNTIME_SETTINGS.md), and [verified commands/results,
artifact checksum and limits](../evidence/A008-0067_engine-package.md).

## Files and verification

Changed engine composition, provider-neutral tool wire types/loop, NVIDIA mapping,
native/MCP runners, ACP/GUI ownership and approvals, global preference migration,
panel activity/authentication, portable packager, actual-package verifier, tests
and owning docs. Companion changes cover generic discovery, runtime host/pool,
narrow permission IPC, panel/dialog components and actual desktop entry wiring.

Root tests: 553 passing (462 + 4 repeated membership + 87 GUI); client: 590 passing.
Both typecheck/build gates pass. Actual fresh ZIP extraction and browser fixture
passed. The browser boundary is explicitly synthetic-provider/component/native-host
integration, not an installed Electron release. No live call or private store used.

## Integration

The ZIP is `C:\code\A008-workers\A008-0067-engine-win32-x64.zip`. Retain its
manifest, runtime, dependency and GUI directories together. Build the companion
client branch to expose the negotiated panels/permissions, discover the extracted
folder, select a project, start its session and open A008 under Brains.
Provider credentials belong to the launching process environment.

Global settings stay outside the bundle. Existing standalone memory can be
attached deliberately through the documented legacy cwd/store variables. Stop
its previous owner first. New project sessions otherwise use isolated engine data.

CURRENT_TASK restored byte-for-byte; archives and evidence recorded. The owner
committed the optional prompt in `66ce0ae`; it remains excluded from the bundle
and is not automatically loaded. The owner-merged 0066
preference task's missing documentation/browser closure is also completed here.
The owner subsequently authorized commit/push/PR. The A008 branch is pushed and
PR #22 is open. The companion change was already merged by another actor in
https://github.com/felixnissen/a007-frontend/pull/104 (`63f845e` on its main).
Append the signed journal on A008 merge. Review/merge and any installed Windows
release remain operator steps; this follow-up did not merge either repository.
