# A008-0132 Handoff — Stage 4 turn identity and ordered snapshot foundation

Status: Complete
Branch: `chatGPT/A008-0132-stage4-turn-recovery-foundation`
Implementation commit: `88afae6`
Parent program: A008-0103

## Delivered

- Stable A008-owned `turnId` for each accepted V2 prompt.
- Stable `messageId` for committed user/assistant messages; unchanged prefixes survive later turns and undo, while removed/reset messages are retired.
- Per-session monotonic event `sequence` plus process `serverInstanceId`, with session/turn identity on event envelopes.
- Ordered inspect snapshot boundary: snapshot captures its represented sequence, later events queue during delivery, then drain afterward.
- Explicit exactly-one terminal settlement as `completed`, `cancelled`, `interrupted` or `failed`, with separate answer and memory status.
- Stale tool permissions fail after turn/session authority changes; disconnect interrupts active work and cancel remains distinct.
- Multi-project/session tests prove independent sequence and turn identity; application IDs stay independent from ACME `modelExecutionId`.

## Verification

- Final branch includes current `main` (including merged A008-0134) before closure.
- `npm run typecheck`: pass.
- `npm run build`: pass.
- `npm run verify:protocol`: pass; packed protocol/dependency installed offline and independent TypeScript consumer compiled/ran.
- `npm test`: 671 core + 4 membership + 171 GUI = 846 passed, 0 failed, 0 skipped.
- Focused Stage-4 regressions cover no-gap snapshot delivery, stale/duplicate event discard, failed/interrupted terminal outcomes, message-ID lifecycle and cross-project/session isolation.
- No live or paid provider call was required or performed.

## Remaining Stage 4 work

A008-0132 is only the first Stage-4 child. Still separate and unclaimed:

1. Command receipts and bounded idempotency.
2. Reconnect/resume capability and lease semantics.
3. Restart/uncertainty and Stage-4 closure proof.

Do not infer exactly-once delivery or command replay from this child.

## Recovery note

`origin/chatGPT/A008-0135-stage4` is a safety snapshot created by the owner and points at implementation commit `88afae6`. It is not a claimed A008 task and must not be merged as A008-0135. The canonical work continues through A008-0132.

## Next step

After this PR merges, claim a new Stage-4 child for command receipts/bounded idempotency. Do not reuse A008-0135 unless it is formally allocated later.
