# A008-0069 handoff

The standalone GUI now has a neutral charcoal palette, left navigation,
centred chat, collapsed runtime/thought details and a rounded composer with a
model-parameter shortcut. Workbench is optional beside Chat; Tools still hosts
Repository, Terminal and Upload. Mobile navigation shows one surface at a time.

Start with `npm run gui` from the repository. Restart an older GUI process if
needed and reload the browser. No provider settings or saved prompts change.

Verification: [evidence](../evidence/A008-0069-focused-gui.md).
Decision: [ADR 0030](../adr/0030-focused-standalone-workspace.md).

The implementation branch is `codex/A008-0069-focused-gui`, based on the owner's
committed A008-0068 checkpoint `fb2f975`. That dependency is not yet on main;
review/merge order must retain A008-0068. No push, PR or merge performed for this
UI task. The task-ID claim was committed locally on main per workflow.
