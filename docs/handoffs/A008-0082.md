# A008-0082 — Independent association lifecycle (L3)

Status: Complete
Date: 2026-09-08
Operator: Codex
Branch: `codex/a008-0082-association-lifecycle`
Implementation head: `27a17081cf99a3fe8f868ffd25161fc8dc5a2be7`
Pull request: none; owner authorized direct main integration/publication.
Blockers: none.

## Result

Accepted P6 is implemented beside RelationIndex. Directed canonical endpoints,
relation type and applicability scope identify independent edge baselines,
occurrence receipts and audit within the project namespace. Creation defaults
are 0.4 strength, 45-day half-life, 0.2 threshold/boost and cap 1. Policy is
snapshotted per operation and stored numerically per edge.

The existing semantic comparator may independently establish an exact edge with
original-source support. Runtime claim/entity/proposal handles resolve locally;
source and unchanged endpoints are checked under the existing commit lock.
Unresolved/unproven updates skip with diagnostics. No new classifier call,
endpoint creation or transfer of strength/truth/state is introduced.

One-hop expansion checks edge activity/applicability separately from endpoint
evidence. Dormant or wrong-scope routes cannot block direct or alternate routes.
Reads/graph inspection never strengthen; existing record detail exposes baseline
and evaluated edge values. Scope variants retain independent records while
sharing a graph line. Model projections remain free of lifecycle metadata.

Knowledge schema 4 adds association persistence in a transaction with the
version change. L2 baselines survive unchanged; schema 1/2 still receive the
accepted L2 conversion. Legacy links remain untracked until exact evidence
initializes metadata for their identity. Settings format 4 reads formats 1-3
and preserves advanced association policy through older-client saves.

## Files and evidence

The implementation commit names all 32 changed files, including documentation
and the restored current-task template. New owners are
[association-lifecycle.ts](../../src/memory/knowledge/association-lifecycle.ts)
and [association-commit.ts](../../src/memory/knowledge/association-commit.ts).
They connect through existing index, live commit/read, SQLite, snapshot, runtime
preferences and inspection owners. No CSS or dependencies changed.

[association-lifecycle.test.ts](../../test/knowledge-model/association-lifecycle.test.ts)
contains ten L3 groups. The
[completed charter](../finished/A008-0082_association-lifecycle.md) maps A25-A30
and P2/P4/P6 to exact evidence.

- `npm run typecheck` and build: passed.
- `npm run test:core`: 521 passed, 0 failures/skips.
- `npm run test:membership`: 4 passed.
- `npm run test:gui`: 116 passed, 0 failures/skips.
- Fixed clocks, exact/directed/scoped target isolation, independent claim/edge
  source spans, attributed reimports, duplicate creation/recurrence, cancellation,
  complete rollback, injected SQLite receipt failure/retry, concurrent connection
  delivery, namespace isolation and reopen/retry.
- Legacy fixture produced through published L2 store d867226; injected DDL/version
  rollback, retry, unchanged evidence baselines in both namespaces, old-store
  rejection of schema 4, backup restoration and corrupt/future-data rejection.
- Existing semantic adapter makes one call per attempt, ignores model numeric
  policy/scope, resolves the actual proposal once and retains projection boundaries.
- Final necessity review: all changes serve the five frozen gate rows. No
  ranking/scaling redesign, recursion, background sweeps or new semantic calls.
- Frozen body/charter, prior immutable records, current template bytes, UTF-8,
  changed-document links/fences, credential patterns and diff checks passed.
  The operator checks this handoff and journal during integration as well.

Not executed: live/paid model judgments, physical power-loss testing, user-data
migration, running-application restart, packaged distribution rebuild or deployment.
Synthetic semantic decisions verify runtime gates, not live model accuracy.

## Operator continuation

Identity c56b88b was published on main before charter 68e1dc5 was frozen. The
implementation and archive are committed; CURRENT_TASK is restored byte-for-byte.
Fast-forward main, publish, journal integration and verify remote/local equality.
The owner's d867226 ASCII-logo CSS remains byte-for-byte intact. Pre-existing
untracked `.grok/` was not read, modified or staged.

L1-L3 now have separate implemented/verified records against the frozen target.
Before a later user-data upgrade, follow the existing
[backup/restore procedure](../KNOWLEDGE_MEMORY_MODEL.md#12-storage-deliberately-deferred).
The running application was not restarted by this task.
