# A008-0091 Handoff

Status: Complete
Date: 2026-09-09
Owner: Codex

## Outcome

A008 now has a renderer-local Session Code Canvas. Completed assistant `html`/`htm` fences render as code and can be opened explicitly as one transient artifact with local Code editing and an isolated HTML/Canvas Preview.

The artifact is content, not repository state. No new host route, ACP method, provider path, native tool or model tool was added. Saving/applying code to the workspace still uses the existing approved `create_file`, `edit_file` or Git tools.

## Safety boundary

Preview uses `iframe srcdoc` with sandbox `allow-scripts` only, no `allow-same-origin`, and an injected restrictive CSP plus connection-API guards. Provider credentials, shell, ACP and host filesystem objects never enter the artifact surface.
## Behavior

- Complete bounded HTML fences only; streaming/incomplete or oversized blocks stay ordinary chat content.
- Artifact source ceiling: 256 KiB UTF-8.
- Local edits rerender Preview without file writes or permission prompts.
- A newer model HTML version advances an untouched artifact; dirty local edits are preserved until the user chooses `Use model update`.
- Reset/new conversation clears transient artifact state.
- `vectorfield.html` was not changed by this task.

## Verification

- `npm --prefix gui run build`: passed.
- GUI suite: 125/125 passed.
- Root suite: 540 core, 4 membership, 125 GUI passed.
- Existing real repository-tool approval regression remained green.
- Final `git diff --check` passed after CURRENT_TASK template restoration.

## Resume

Optional later work should be separate tasks: multi-file artifacts, durable artifact persistence, or an explicit export/apply UI.