# A008-0070 handoff

The Chat Workbench button opens a floating Environment/Sources card: git branch
and `+N −M` from host-shell git observations, session uploads, clipboard ingest
via `POST /v1/upload`, and commit/compare prompts. The old tool catalog lives
on Help. Empty chat offers Review, Terminal, Browser, Files and Workbench
shortcuts. Tools keeps those working panes. The Memory Relationship Map is a
domain-clustered radial graph with a structured inspector.

Start with `npm run gui` and reload. Connect as usual. Workbench git counts
need the GUI host; Help and the empty shortcuts do not.

Verification: [evidence](../evidence/A008-0070_workbench-memory-map.md).
Decision: [ADR 0031](../adr/0031-workbench-context-and-memory-map.md).

No push, PR, merge or live provider call.
