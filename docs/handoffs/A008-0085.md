# A008-0085 — Extraction JSON contract and regression

Status: Complete
Date: 2026-09-08
Operator: Codex
Branch: codex/extraction-json-contract
Integration: owner-authorized direct main publication; no pull request.
Head: implementation SHA is recorded by the operator in JOURNAL on integration.

## Result

The reported failure is invalid semantic output, not an HTTP rate-limit response.
Its excerpt has unquoted names/values, single-quoted strings and an unclosed
proposition string. The capped trace stops before the failing request, so it
cannot establish the full response or selected extraction model. The visible
retrieval model is a separate owner and does not identify the extractor.

The existing extractor support example used pseudocode. That is a concrete
instruction defect and a plausible contributor, not a proven generation cause.
It now uses typed prose and serialized fictional examples for greeting-only,
mixed durable fact and source inputs. Completeness applies to reusable claims;
the model still owns semantic selection. Relation association support likewise
uses typed prose. Invalid-response diagnostics include model and operation.

Strict parsing, one-call ownership, source fidelity and no-write-on-failure
remain intact. There is no repair, retry, lexical filter, model switch or new
provider parameter. A008-0083's Kimi parameter correction remains in place.

## Files and verification

- Runtime owner: src/orchestration/semantic-json-model.ts.
- Regression owners: test/semantic-json-model.test.ts,
  test/post-output-knowledge-intake.test.ts and test/runtime-preferences.test.ts.
- Prepared check: [check-semantic-extraction.mjs](../../scripts/check-semantic-extraction.mjs).
- Owning docs: CURRENT_STATUS, SYSTEMDOC, SEMANTIC_JSON_MODEL_CALLS, FILESTRUCTURE,
  task/archive, current-task template and this handoff.

Passed commands:

- `npm run typecheck`
- `npm run test:core` — build and 528/528 tests, zero skips.
- `npm run test:gui` — 118/118 tests, zero skips.
- `npm run test:membership` — 4/4 tests, zero skips.
- `node --check scripts/check-semantic-extraction.mjs`
- `node scripts/check-semantic-extraction.mjs --model moonshotai/kimi-k3`
  — credential-free dry-run only, no provider call.
- `git diff --check`; final necessity, frozen charter/spec, unchanged GUI/logo,
  dependency files, earlier archives/ADRs and new documentation links reviewed.

All three prompt example arrays strict-parse and stage with zero/one/one
proposals, no skipped proposals and exact original-source support. Both reported
malformed forms still fail in one call. An isolated real runtime keeps the
already-delivered answer, reports staging_failed, writes zero memory records and
makes no classifier or retry call. These fake-transport tests establish local
contracts, not live semantic judgement or future model compliance.

## Pending live verification

No live-provider authority or failing-model confirmation was received at closure.
The task's frozen Definition of Done permits this explicit pending handoff.
Do not infer either answer from the dry-run model or the retrieval trace.

After the owner confirms the model and authorizes the prepared check, build then
run the script with that exact registered `--model` and `--live`; credentials
come only from the existing environment (Node may load `.env.local`). The
[NVIDIA check procedure](../SEMANTIC_JSON_MODEL_CALLS.md) describes the request.
It sends at most three calls, at most 16384 output tokens per call, no retries
and no database writes. Cases are a Swedish greeting, a mixed language preference
and a synthetic SQLite storage statement. Review the printed synthetic claims
as well as syntax, count and span checks; keyword checks alone do not establish
correct meaning. Record aggregate results in a new journal entry/current status;
do not commit raw responses or reasoning or rewrite immutable evidence.

If the model still returns malformed JSON, preserve strict rejection and route
the new evidence through the existing task workflow. No provider-side structured
output compatibility has been established by this correction. Real-store review
and application restart were not performed.
