# A008-0134 Handoff — validated zero-cost model catalog

Status: Complete
Branch: `A008-0134-zero-cost-model-catalog`
Primary deliverable: `src/providers/zero-cost-model-catalog.ts`

## Delivered

- 26 provider/model routes across NVIDIA Build, OpenCode Zen, OpenRouter, Groq and Google Gemini.
- Provider-route semantics distinguish `free-endpoint`, `free-model` and `free-tier`.
- Lifecycle metadata distinguishes recurring, dynamic, preview and trial access.
- Exact model IDs, OpenAI-compatible base URLs/API styles, known context/output limits, quotas/expiry, sensitive-data policy, optional existing A008 profile IDs and provider-source URLs are recorded.
- Six direct NVIDIA entries correspond to profiles already present in A008. All other entries are catalog-only.
- No runtime registration, dispatch, ACME, credential, default-model or fallback behavior changed.

## Verification

- Provider-owned current documentation revalidated 2026-09-19.
- `npm run typecheck`: pass.
- `npx prettier --check src/providers/zero-cost-model-catalog.ts`: pass.
- `npm run build --silent`: pass.
- Compiled-catalog invariant check: 26 routes, 5 providers, unique route keys and unique provider/model pairs.
- No live provider inference or paid call performed.

## Important consumption rules

- Revalidate dynamic/preview/trial entries before unattended use.
- Never silently substitute a paid sibling when a free route disappears.
- Treat `dataPolicy: "do-not-send-sensitive-data"` as a hard routing exclusion for confidential/personal payloads.
- Groq rows represent Free Plan quota, not globally zero-priced models.
- Dots3-Note Preview has an explicit `expiresAt: "2026-09-30"`.
- A catalog entry does not imply A008 runtime support; add provider/runtime support only in a separately authorized task.

## Next step

A later provider/routing slice can import `ZERO_COST_MODEL_ROUTES` and select a bounded subset for implementation without re-parsing ZeroCostRadar.
