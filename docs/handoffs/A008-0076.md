# A008-0076 handoff

Empty chat has a transparent 4D starfield behind the ASCII mark. It stops when
a message or generated image appears. Mouse movement tilts the 4D planes.
`prefers-reduced-motion: reduce` skips the loop.

Verification: [evidence](../evidence/A008-0076_empty-starfield.md).
