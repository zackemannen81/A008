# A008-0183 Handoff

## Completed

- Added a renderer-local top application menu: File, Edit, View and Help.
- Added a persistent desktop sidebar width (208–480 px), pointer drag splitter with `col-resize` affordance, and keyboard separator controls.
- Added persistent hide/show sidebar controls without altering mobile navigation or host/runtime ownership.

## Verification

- `npm run typecheck` — pass.
- `npm --prefix gui run typecheck` — pass.
- Focused sidebar/shell tests — 6/6 pass.
- `npm --prefix gui run test` — 207/208 pass.
- `git diff --check` — pass.

## Known limitation

- The sole GUI-suite failure is pre-existing: `gui/src/settings/parameters-panel.test.ts` reads source files from the GUI root instead of `src/settings`. It was intentionally not changed by this sidebar/menu slice.

## Follow-up

- Skills activation/deactivation remains a separate bounded task.
