# Handoff — A008-0114

Status: Complete; Stage 3.5 complete; default-route **NO-GO**
Task ID: A008-0114
Parent: A008-0103
Branch: `grok/A008-0114-acme-execution-evaluation`

## Outcome

A008 can talk to frozen `acme-model-runtime/1` through `AcmeChatTransport`.
Selection is explicit (`A008_CHAT_TRANSPORT=acme` +
`A008_ACME_MODEL_RUNTIME_URL`). Direct NVIDIA/kie/OpenAI transports remain the
default and the authoritative path.

Parity and failure-evidence tests pass. Adoption as the normal execution
substrate is **NO-GO**: ACME `ModelRequest` cannot carry A008 thinking,
reasoning budget, topP, seed or reasoningEffort. Stage 4 proceeds without ACME.

## Verification

- 626 core + 4 membership + 162 GUI tests passed.
- Protocol package proof, production GUI build, portable engine proof and
  `git diff --check` passed.
- No live provider call.

## Continuation

A008-0103 Stage 4 is next: turn/message identity, snapshot/event boundary,
terminal outcomes, command receipts/idempotency, reconnect/resume. Do not
conflate those application identities with ACME `modelExecutionId`. Do not
start Stage 4 on this branch.

A later task may revisit ACME default-route adoption only after ACME
`ModelRequest` can represent A008 generation controls, or after an explicit
owner decision to accept that subset.
