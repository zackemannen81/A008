# Handoff — A008-0093

Status: Complete, ready for operator merge
Task ID: A008-0093
Branch: `grok/A008-0093-global-app-theme-system`
Worktree: `C:\code\A008-workers\A008-0093_global-app-theme-system`
Base: `6bf8445` (`docs: claim global app theme system`)
Head: `eb28328` (`feat: add Neutral and Deep Space app themes`)

## Outcome

The standalone GUI has a persistent semantic theme system. Neutral is the
extracted current charcoal look. Deep Space is a second named expression
(blue-black/navy, restrained electric-blue interaction). TypeScript owns theme
identity; CSS owns values. Components do not branch on theme id.

## Files

- `gui/src/brand/themes.css` — Neutral and Deep Space token values
- `gui/src/brand/theme.ts` — identity, parse, required token lists, root attribute
- `gui/src/brand/theme-storage.ts` — `a008.preferences` appearance document
- `gui/src/brand/theme.test.ts` — parse/default/persistence/DOM/CSS contract
- `gui/src/brand/a008.css` — existing names alias the semantic model
- `gui/index.html` — early `data-a008-theme` boot
- `gui/src/settings/appearance-panel.tsx` — Parameters → Appearance → App theme
- Memory/graph, Code Canvas chrome, browser pane, workspace/parameters CSS retokenized
- `gui/test/memory-map-preview.mjs` — optional `theme=deep-space` preview

## Verification

- `npm test`: 543 core / 4 membership / 143 GUI, zero failures
- `npm --prefix gui run build` passed
- `git diff --check` passed
- Browser screenshots in `docs/evidence/A008-0093_app-theme.md`
- No provider call, no `settings.json` write, no ACP/host protocol change

## Safety

Theme is renderer-local presentation. Runtime settings, session, memory, tools
and the sandboxed Code Canvas preview document are unchanged.

## Docs

`CURRENT_STATUS`, `SYSTEMDOC`, `FILESTRUCTURE`, evidence, finished archive.
`CURRENT_TASK.md` restored from the template. Journal is for operator merge.

## Next

Review and merge to `main`. OLED/Warm/High Contrast/Custom and a selectable
visualization palette remain out of this charter.
