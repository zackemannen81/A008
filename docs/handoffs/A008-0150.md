# A008-0150 Handoff — optional MCP null sentinels

Status: Complete
Task: A008-0150
Branch: `a008-0150-optional-mcp-null-sentinels`
Implementation base: main `3ab94ec`
Pull request: pending

## Delivered

- `ModelToolSession` normalizes OpenAI strict-tool omission sentinels before the
  existing original MCP-schema Ajv validation.
- It removes a top-level `null` only if the original MCP property is optional
  and non-nullable.
- Required properties and properties whose original schema accepts `null` remain
  unchanged and continue through the existing fail-closed validation.
- The same normalized object reaches approval and the MCP server.
- Regression coverage includes a real stdio MCP fixture that receives
  `{ url: "https://example.com" }` from a strict-provider-shaped input with
  `restore: null`.

## Verification

- `npm run typecheck`: PASS.
- `npm run build`: PASS.
- `node --test dist/test/model-tools.test.js`: 12/12 passed.
- Changed-file Prettier check: PASS.
- `git diff --check`: PASS.
- `npm test`: 711 passed, 1 failed, 0 skips. The unrelated,
  reproducible failure is `runtime-preferences.test.ts` real host/ACP control
  coverage, which expected `prompt/ok` and received `error`; its isolated run
  also fails 13/14. This task does not touch that path.
- Repository-wide `npm run format:check` reports 67 pre-existing unrelated
  formatting failures; changed files pass targeted formatting.
- No live/paid provider call.

## Constraints kept

- Original MCP schemas remain the authoritative execution validator.
- No ACME/OpenAI/provider or MCP schema transformation changes.
- No broader argument coercion, approval, cancellation, transport or budget
  behavior changes.

## Next action

Open and review the PR. If a green full-suite baseline is needed, charter the
reproducible runtime-preferences host/ACP failure separately rather than
expanding this completed hotfix.
