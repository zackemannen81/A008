# A008-0141 Handoff — Oldscool CRT scanlines and phosphor bloom

Status: Complete
Parent Task: none

## Delivered

- Added Oldscool as the third persistent renderer-local app theme under Parameters → Appearance.
- Oldscool applies before React mounts and persists in the existing `a008.preferences.appearance.theme` value.
- Added dark CRT/olive surfaces, phosphor-green controls, warm retro highlights and complete visualization tokens.
- Added one static `aria-hidden`, pointer-inert scanline overlay rendered by `gui/src/app.tsx`; only `html[data-a008-theme="oldscool"]` displays it.
- Added restrained phosphor bloom to Oldscool chrome, composer/settings primary controls and non-quiet Memory buttons; Neutral/Deep Space have no matching selectors.
- Kept the sandboxed Code Canvas preview independent of host theme behavior.

## Verification

- `npm --prefix gui run test -- --test-name-pattern="theme"`: 177 passed, 0 failures/skips.
- `npm --prefix gui run typecheck`: passed.
- `npm --prefix gui run build`: passed.
  - Existing warnings only: two Zod annotation comments and a >500 kB minified chunk.
- `git diff --check`: passed.

## Boundaries

No session, provider, model, tool, memory, host/API, graph semantic, layout, navigation or Code Canvas-preview behavior changed. No live provider call was made.

## Next step

The requested local commit is ready for the operator to inspect and push. No PR was opened and no remote push was performed.
