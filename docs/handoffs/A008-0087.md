# A008-0087 handoff

GPT-5.6 Luna is implemented as a first-class OpenAI Chat Completions provider.
The built-in `gpt-5.6-luna` profile supports the existing A008 tool approval loop,
streaming, memory-aware chat and stateless semantic calls. OpenAI credentials are
host-owned/write-only via environment or Provider settings and never returned to
the renderer.

Offline acceptance: 539 core, 4 membership and 119 GUI tests pass; GUI production
build and `git diff --check` pass. A dedicated OpenAI-only runtime regression
shows retrieval scope, chat and knowledge analysis all hitting OpenAI with no
NVIDIA request. Chat defaults to reasoning effort `medium`; semantic JSON calls
use `none`.

No live OpenAI API call was performed. The operator-created API key is not in Git;
enter it through Parameters → Provider, then select `gpt-5.6-luna` for live use.
