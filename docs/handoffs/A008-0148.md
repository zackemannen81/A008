# A008-0148 Handoff

## Completed

- Added canonical persisted stdio MCP server definitions to the existing user catalog.
- Added Parameters → MCP for inspecting, adding, editing, disabling and removing definitions.
- Added typed authenticated V1 `GET`/`POST /v1/mcp-servers` routes and regenerated protocol OpenAPI.
- Passed enabled definitions only into newly constructed V1 bridge and V2 EngineHost sessions.
- Retained EngineHost → ModelToolSession as the sole MCP process, catalog and tool owner.
- Added real-host HTTP contract coverage for valid and invalid MCP catalog requests.

## Lifecycle and Safety

Changes apply only when a new tool/session runtime is created. Active sessions retain their existing MCP catalog. The renderer never spawns MCP processes. stdio remains the only accepted transport; existing approval, cancellation, timeout and catalog/tool budgets remain ModelToolSession behavior.

## Verification

- Root typecheck passed.
- GUI typecheck passed.
- Core suite: 703/703 passed.
- Core membership suite: 4/4 passed.
- GUI suite: 187/187 passed.
- `git diff --check` passed.

## Integration Note

A008-0148 owns configuration-to-session construction input only. It neither restores nor migrates conversations/sessions and must not compete with A008-0147 lifecycle ownership.
