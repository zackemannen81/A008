# A008-0147 Handoff — Project conversation session restore

Status: Complete
Task: A008-0147
Parent Task: none
Branch: `chatGPT/A008-0147-session-restore`
Claim base: `97cf8c2`
Final integration base: `7df08e3` (A008-0148 / PR #87 merged)

## Delivered

- Standalone workspace project conversation is durable in the project's existing SQLite owner.
- The durable state contains conversation identity, selected chat model and canonical committed multimodal messages.
- Project A → B → A reconstructs A's transcript without leaking it to B.
- Host/runtime restart reconstructs the same project conversation under a new ephemeral session identity.
- Generic EngineHost/ACP/V2 sessions remain fresh and independent; restore is an explicit standalone-workspace composition mode.
- Reset, undo, explicit model change, committed turns and generated-image lifecycle changes write through from canonical runtime/session state.
- Completed source-store image locators survive restore.
- Stale persisted pending image generation is terminalized as cancelled on restore and is never replayed.
- ADR 0046 records that durable project conversation state supersedes ADR 0041's blanket no-durable-chat statement only for this workspace surface; session/resume/receipt/tool/thought/permission authority remains process-local.

## Verification

- Focused restore/runtime suite: 19/19 pass.
- Final post-rebase `npm test`: 707 core + 4 membership + 187 GUI = 898 pass, 0 failures/skips.
- `npm run typecheck`: pass.
- `npm --prefix gui run build`: pass; existing Zod annotation and >500 kB chunk warnings only.
- `npm run verify:protocol`: pass with packed protocol/dependency and independent offline TypeScript consumer.
- `git diff --check`: pass.
- No live/paid provider call.

## Boundaries

- No MCP configuration/lifecycle work; that was implemented independently in parallel.
- No assistant-final/post-output-memory decoupling or activity-animation work.
- No durable ACP/V2 session IDs, resume capabilities, command receipts, tools, thoughts or permissions.
- No multiple named conversations, offline sync, cross-device sync or concurrent workspace writers.
- No provider/memory architecture redesign.

## Integration note

A008-0148/PR #87 was merged to `main` before final publication. A008-0147 rebased onto that exact main without conflict. The combined suite passes 898/898, including the new MCP settings/lifecycle coverage and the workspace-only restore/generic-session freshness proofs.

## Next action

Operator merges this branch and appends the signed `docs/JOURNAL.md` entry. No MCP conflict remains to resolve. Workers do not append the journal or merge their own branch.
