# Handoff — A008-0028

Status: Ready for operator merge

Task ID: A008-0028
Parent Task: A008-0021
Owner: A008-worker03
Branch: `grok/A008-0028-storage-redesign`
Base: `origin/main` (`e450cfa`)
Worker clone: `C:\code\A008-workers\A008-worker03`

## Head commit SHA

Implementation (code + docs + archive + restored CURRENT_TASK template):

`030def67c2bbe0ff2ef0c4fee30962048390216d`

This handoff file is a follow-up commit on the same branch.

## Pull request URL

https://github.com/zackemannen81/A008/pull/TBD

Do not merge from this worker. Operator is the only merger to `main`.

## Files changed

Implementation commit:

- `src/memory/knowledge/sqlite-schema.ts`
- `src/memory/knowledge/sqlite-store.ts`
- `src/memory/knowledge/sqlite-context.ts`
- `src/memory/knowledge/live-reader.ts`
- `src/memory/knowledge/live-commit.ts`
- `src/memory/knowledge/state.ts` — hydrate / transitionSequence
- `src/memory/knowledge/evidence.ts` — hydrate
- `src/memory/knowledge/lifecycle.ts` — hydrate / transitionSequence
- `src/memory/knowledge/registry.ts` — hydrate
- `src/memory/knowledge/expand.ts` — exportLinks / hydrate
- `src/memory/knowledge/index.ts` — exports
- `src/runtime/local-memory-runtime.ts` — live cutover
- `src/index.ts` — public exports
- `test/knowledge-model/scenarios.test.ts` — in-memory + SQLite S1–S10
- `test/knowledge-model/sqlite-store.test.ts`
- `test/local-memory-runtime.test.ts`
- `package.json` — include sqlite-store tests
- `docs/SEMANTIC_MEMORY.md`
- `docs/SYSTEMDOC.md`
- `docs/CURRENT_STATUS.md`
- `docs/FILESTRUCTURE.md`
- `docs/LOCAL_MEMORY_SURFACES.md`
- `docs/finished/A008-0028_storage-redesign.md`
- `docs/CURRENT_TASK.md` — restored from `docs/template_CURRENT_TASK.md`

Handoff commit:

- `docs/handoffs/A008-0028.md` (this file)

Not changed (charter out of scope):

- `src/memory/types.ts` (`KnowledgeItem` not grown)
- OpenHands, `.env.local`
- `docs/JOURNAL.md`

## What landed

M7 storage redesign. Persist the accepted model; point live CLI/ACP at it.

- Separate SQLite families: artifacts, entities, slots, bindings (intervals),
  transitions, slot claims, events, utterances, evidence claims, provenance,
  lifecycle, relations. FTS over labels. Slot open/closed index.
- Unknown interval boundaries stored as `{ unknown: true }`, never `now`.
  Open current is `valid_to` SQL NULL. Unknown close is JSON unknown, not NULL.
- V0 supersede chains migrate into those intervals with unknown boundaries.
  Lifecycle stays on migrated evidence, not on bindings.
- S1–S10 pass in-memory and against SQLite with identical projection payloads.
  Close/reopen preserves S1 current and history answers.
- Live `createLocalMemoryRuntime` uses `KnowledgeMemoryReader` and
  `KnowledgeEngineCommit`. Classifier is a comparator. ACCEPT is
  `user-assertion-v1`. UPDATE writes no lifecycle (no V3). Direct matches
  ignore dormancy (no V4). PROJECT writes nothing (no V7).

`KnowledgeItem` remains a compatibility surface. Live CLI/ACP do not write it.

## Exact verification commands and results

Working directory: `C:\code\A008-workers\A008-worker03`
Date: 2026-09-02

### Full project typecheck — pass

```text
npm run typecheck
```

Exit 0.

### Named scenario suite — pass

```text
npx tsc -p tsconfig.json
Remove-Item Env:FORCE_COLOR -ErrorAction SilentlyContinue
node --test dist/test/knowledge-model/scenarios.test.js dist/test/knowledge-model/sqlite-store.test.js
```

In-memory and SQLite S1–S10, decay sweep, identical-payload, close/reopen,
unknown-boundary, and v0 migration tests passed.

### Full project tests — pass

```text
Remove-Item Env:FORCE_COLOR -ErrorAction SilentlyContinue
npm test
```

```text
ℹ tests 210
ℹ pass 210
ℹ fail 0
```

Previous suite was 189.

### `git diff --check` — pass

No whitespace errors.

### No live provider / `.env.local` / OpenHands

No such files read or mutated. No paid call.

## Skipped gates and reasons

- `docs/JOURNAL.md` was not appended. Operator appends on merge.
- Live NVIDIA and OpenHands mutation remain out of scope.
- Merge to `main` is operator-only.

## Blockers

None for merging this slice on review.

## Next recommended operator action

1. Review the pull request
2. Merge to `main` when satisfied (operator only)
3. Append the signed `docs/JOURNAL.md` entry on merge
4. Parent A008-0021 can complete D9 after this merge
