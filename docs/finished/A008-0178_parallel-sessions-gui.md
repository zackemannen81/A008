# A008-0178 ? Parallel sessions GUI

Task ID: A008-0178
Parent Task: A008-0177
Status: Complete
Owner: Rickard (operator)
Completed: 2026-09-24
Charter frozen at: 2026-09-24

## Delivered

- Added authenticated V1 host/session routes and typed client contracts for isolated project worktree sessions and the external configurable worktree root.
- Connected the existing `ProjectWorkspaceStore` to `GuiWorkspaceStore`, preserving its literal-Git clean-only discard behavior.
- Added project-details GUI controls that show branch, base, path, changed-file count and commits ahead, and require explicit confirmation to create, keep or discard.
- Kept Merge and Create PR unavailable. No remote mutation, branch deletion or automatic merge was added. Opening an active worktree explicitly binds its cwd for the next chat/tool session.

## Verification

- `npm run typecheck -- --pretty false` passed.
- `npm run build`, `npm run build:client`, and `npm --prefix gui run build` passed.
- `npm --prefix gui run test` passed: 204/204.
- `node --test dist/test/project-workspace-store.test.js dist/test/gui-host.test.js dist/test/http-contract.test.js` passed 46/48; OpenAPI drift was then regenerated by `npm run protocol:schemas`, and its schema test passed. The remaining `http-contract` real-host cleanup failed only with Windows `EPERM` removing its temporary directory after its assertions.
- `git diff --check` passed.
- No live provider calls; 0 SEK.

## Handoff

The workspace-root setting is stored outside project repositories beside the project registry by default. The open action changes the next bridge cwd explicitly; merge, remote push, PR creation and branch deletion remain future work.
